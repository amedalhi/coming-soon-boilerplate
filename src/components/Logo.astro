---
import config from '@config/config.json';

const { imageLogo, textLogo} = config.header;

const logo = imageLogo ? "image logo here" : textLogo
---

<p>{logo}</p>

<style>
    p {
        font-size: var(--step-1);
        font-weight: 700;
        letter-spacing: -0.75px;
    }
</style>